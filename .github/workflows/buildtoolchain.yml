name: Examples

on: [push, pull_request]

jobs:
  buildlinux:
    strategy:
      fail-fast: false
      matrix:
        architecture: [avr, arm-none-eabi, m68k-elf]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: "recursive"        
      - name: Install dependencies
        run: |
          sudo apt-get install wget bzip2 xz-utils unzip git make tar flex bison diffutils texinfo gcc g++ libexpat-dev
        shell: bash
      - name: Build toolchain
        run: |
          bash buildtoolchain.sh linux  ${{ matrix.architecture }}
      - name: Build example with toolchain
        run: |
          bash runtests.sh  ${{ matrix.architecture }}
      - name: Build platformio examples with toolchain
        run: |
          python -m pip install --upgrade pip
          pip install -U https://github.com/platformio/platformio/archive/develop.zip
          bash runtests_pio.sh  ${{ matrix.architecture }}
      - name: 'Upload Artifact'
        uses: actions/upload-artifact@v4
        with:
          name: linux-${{ matrix.architecture }}
          path: toolchain-${{ matrix.architecture }}-linux-gcc-15.2.0.tar.gz
          retention-days: 5    

  buildalpine:
    strategy:
      fail-fast: false
      matrix:
        architecture: [avr, arm-none-eabi, m68k-elf]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: "recursive"
      - name: Setup latest Alpine Linux
        uses: jirutka/setup-alpine@v1          
      - name: Install dependencies in alpine
        run: |
          cat /etc/alpine-release
          apk add bash wget git build-base m4 texinfo bison flex gawk patch expat expat-dev 
        shell: alpine.sh --root {0}
      - name: Build toolchain in alpine
        run: |
          bash buildtoolchain.sh alpine  ${{ matrix.architecture }}
        shell: alpine.sh {0}
      - name: Build example with toolchain in alpine
        run: |
          bash runtests.sh  ${{ matrix.architecture }}
        shell: alpine.sh {0}
      - name: 'Upload Artifact'
        uses: actions/upload-artifact@v4
        with:
          name: alpine-${{ matrix.architecture }}
          path: toolchain-${{ matrix.architecture }}-alpine-gcc-15.2.0.tar.gz
          retention-days: 5    


  release:
    permissions:
      contents: write
    needs: [ buildalpine, buildlinux ]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*.tar.gz
          generate_release_notes: true

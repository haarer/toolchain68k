#!/bin/sh
# install fedora package requirements, based on a minimum install 
#( install sudo first and add your user to the sudoers)
sudo yum install redhat-lsb-core git make tar flex bison diffutils texinfo gcc wget bzip2
